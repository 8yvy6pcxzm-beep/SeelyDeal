import TemplatesPage from "@/app/(app)/templates/page";

/** The real Templates page is already 100% demo-data driven (lib/demo/data.ts), no auth needed — reused as-is. */
export default function DemoTemplatesPage() {
  return <TemplatesPage />;
}
